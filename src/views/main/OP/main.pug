//- Brand-specific markup for OP.
//-
//- The module's shared skeleton lives in src/views/main/main.pug. Add here only
//- what differs for this brand; the file is rendered without pug locals, so no
//- `bannerName` / `crossoriginAttr` are available.
//-
//- OP needs no brand-specific markup: the four cards are identical across brands
//- and only the design tokens (src/scss/variants/OP/_variables.scss) and the copy
//- (src/json/variants/OP/json.json) change.
