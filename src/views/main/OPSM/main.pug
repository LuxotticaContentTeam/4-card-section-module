//- Brand-specific markup for OPSM.
//-
//- The module's shared skeleton lives in src/views/main/main.pug. Add here only
//- what differs for this brand; the file is rendered without pug locals, so no
//- `bannerName` / `crossoriginAttr` are available.
//-
//- OPSM needs no brand-specific markup: the four cards are identical across brands
//- and only the design tokens (src/scss/variants/OPSM/_variables.scss) and the copy
//- (src/json/variants/OPSM/json.json) change.
