//- Brand-specific markup for SV.
//-
//- The module's shared skeleton lives in src/views/main/main.pug. Add here only
//- what differs for this brand; the file is rendered without pug locals, so no
//- `bannerName` / `crossoriginAttr` are available.
//-
//- SV needs no brand-specific markup: the four cards are identical across brands
//- and only the design tokens (src/scss/variants/SV/_variables.scss) and the copy
//- (src/json/variants/SV/json.json) change.
